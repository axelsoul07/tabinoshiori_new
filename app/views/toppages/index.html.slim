- if logged_in?
  .row
  aside.col-md-4
    = form_for(@plan) do |f|
      .form-group
        = f.text_field :title, class: 'form-control', rows: 5
      = f.submit '旅の栞作成', class: 'btn btn-primary btn-block'
  .col-xs-8
    = render 'plans/plans', plans: @plans
- else
  .row
  .center.jumbotron.trans_back
    .text-center
      h1
        | 旅の栞が生みだす楽しみ
      = link_to 'ユーザ登録はこちらから！', signup_path, class: 'btn btn-lg btn-primary'
